#!/usr/bin/env bash

source ../config

SOCKET_WRAPPER_LIBRARY_NAME=libsocketwarpper.so
SOCKET_WRAPPER_LIBRARY_PATH="$BUILD_DIR/examples/$SOCKET_WRAPPER_LIBRARY_NAME"

DCCS_SOCKET_LIBRARY_NAME=libdccssocket.so
DCCS_SOCKET_LIBRARY_PATH="$BUILD_DIR/socket/$DCCS_SOCKET_LIBRARY_NAME"

MPI_DAEMON_EXECNAME=mpi_daemon
MPI_DAEMON_EXECPATH="$BUILD_DIR/transport/mpi/$MPI_DAEMON_EXECNAME"

